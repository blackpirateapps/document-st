// Basic placeholder test for Flutter project.
// Full widget tests will be added once the app is stable.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Placeholder test - app compiles', () {
    // This test exists to satisfy Flutter's expectation of a test file.
    // It verifies that the test framework itself is working.
    expect(1 + 1, equals(2));
  });
}
